import { View } from "react-native";
import { separatorStyles } from "./Separator.component.style";

const Separator = ({ darkMode }) => {
  return <View style={separatorStyles}></View>;
};

export default Separator;
